---
name: contract-update
description: 마크업 계약서(docs/contract.md)를 개정한다. 새 클래스·구조·토큰이 필요할 때 계약서를 먼저 고치고 영향받는 CSS/HTML/JS를 함께 맞춘다. "구조 바꾸자", "새 컴포넌트 추가", "클래스명 변경", "계약서 갱신" 같은 요청에서 사용한다.
---

# 마크업 계약서 개정

`docs/contract.md`는 디자이너와 두 개발자가 동시에 작업할 수 있게 해주는 **단일 진실 공급원**이다. 계약서 없이 클래스를 추가하면 CSS는 있는데 HTML엔 없거나, 그 반대가 된다.
**대상은 클래스·구조만이 아니다**(CLAUDE.md 규칙 6): 사용자에게 보이는 문구 · 키 동작 · 저장 키가 바뀔 때도 이 절차를 먼저 밟는다. 키 동작은 **동작과 합격 기준**(입력 → 기대 결과)으로 적고 구현 공식은 적지 않는다(meeting-08 R-C).

## 소유권

**계약서는 `web-designer`가 소유한다.** 개발자가 필요를 느끼면 요청하고, 디자이너가 판단해서 개정한다.
PM은 개정의 타당성을 검토할 수 있지만 직접 고치지 않는다.

## 개정 절차 (순서를 지킬 것)

### 1. 변경 이유를 한 문장으로
"필요해서"는 이유가 아니다. **어떤 화면의 어떤 문제를 해결하는가**를 적는다.
기존 클래스로 해결되지 않는지 먼저 확인한다 — 계약서가 비대해지는 것이 가장 큰 위험이다.

### 2. 영향 범위 조사 (개정 전에)
```bash
grep -rn "기존클래스명" css/ js/ *.html docs/
```
이름을 바꾸는 개정이면 **쓰이는 곳을 전부 찾아둔다.** 하나라도 놓치면 스타일이 빠진 요소가 생긴다.

### 3. 계약서 수정
해당 섹션에 새 마크업 블록을 추가하거나 기존 것을 고친다. 규칙:
- 클래스명은 기존 컨벤션을 따른다: 블록 `.memo`, 요소 `.memo-title`, 상태 `.is-active`
- 상태 클래스는 정해진 목록(`is-active is-open is-stuck is-hidden is-dirty is-loading is-pinned is-visible`)에서 고른다. 새 상태가 꼭 필요하면 그 목록에도 추가한다
- 토큰을 추가하면 토큰 섹션에도 반드시 등재한다
- 문서 맨 위 버전을 올린다(`v1` → `v2`)

### 4. 변경 요약 작성
개정 보고에 **변경 전 / 변경 후**를 나란히 적는다. 개발자는 이 diff만 보고 코드를 맞춘다.

### 5. 후속 작업 분배
| 변경 종류 | 후속 담당 |
|---|---|
| 새 컴포넌트 | 디자이너가 CSS, 담당 개발자가 마크업 |
| 클래스명 변경 | 디자이너 CSS + 해당 파일 소유 개발자 동시 |
| 토큰 추가 | 디자이너 단독 |
| 데이터 필드 추가 | frontend-dev(파서·렌더) + frontend-dev-2(에디터 입력) 둘 다 |

**데이터 필드 추가는 특히 주의한다** — 에디터에서 입력받지 못하면 사용자가 그 필드를 영영 못 쓴다.

### 6. 정합성 확인
개정과 구현이 끝나면 `/code-audit`으로 CSS↔HTML 클래스 정합성을 검사한다.

### 7. 계약서 자체 일관성 grep (개정할 때마다 — 빌드·린터가 없으니 이게 린터다)
절대 규칙 1로 CI·린터가 하던 일관성 검사도 함께 사라졌다(meeting-08 R-F). 개정을 끝내기 전에 아래를 돌려 **전부 기대값**이어야 보고한다.
```bash
C=docs/contract.md
grep -n '^### v' "$C" | awk '{print $2}' | sort | uniq -d          # 버전 소제목 중복 — 빈 출력
head -1 "$C" | grep -o 'v[0-9.]*'; awk '/^## 13/{f=1} f&&/^### v/{print $2; exit}' "$C"   # 두 값이 같아야 한다(머리말 버전 = §13 맨 위, §13은 최신이 위)
for n in <이번에 폐기한 이름들>; do printf '%s ' "$n"; grep -c -- "$n" css/*.css js/*.js *.html | awk -F: '{s+=$2} END{print s}'; done   # 코드에서 0
grep -n '현재 write.html\|현재 index.html\|현재 post.html' "$C"    # "현재" 표기가 최신 버전만 가리키는지 눈으로
```
- **머리말 ↔ 본문**: 머리말의 "바뀐 절" 목록과 "CSS/마크업 변화 없음" 같은 요약 문장이 실제로 고친 절과 맞는지(meeting-08 D10 — v4.2 머리말이 "CSS 변화 없음"이라 적고 바로 아래서 CSS를 추가했다).
- 폐기한 클래스·키·문구가 계약서 **다른 절**에 현행처럼 남았는지 — `grep -n -- "<폐기 이름>" "$C"`로 나온 줄이 전부 취소선·이력 절이어야 한다.
- 결과(명령과 출력 요약)를 개정 보고의 "검증 결과"에 붙인다.

## 하지 말 것

- 코드를 먼저 고치고 계약서를 나중에 맞추기 → 이러면 계약서가 죽은 문서가 된다
- "일단 임시로" 계약서 밖 클래스 쓰기 → 임시는 영원히 남는다
