# 라운드 8 통합 회의 (2026-09-23)

사용자 원문: *"에이전트 통합 전체 회의 돌려(한 자리에 지금 존재하는 모든 에이전트들 끼리 의견을 내고 반박문 및 Why? 전법으로 계속 모순점과 개선점, 추가할 부분을 찾아서 회의록으로 정해)"*

- **참석 5인**: pm-integrator · web-designer · frontend-dev · frontend-dev-2 · **tutor**(학습자·글쓴이 관점, 첫 참석).
- **진행**: 1R 발제(5인 병렬 감사, 항목마다 파일:줄 + Why 2단 이상) → 2R 반박(5인이 서로의 1R 전부를 읽고 동의·반박·보강·중복 판정, 자기 반박, Why 한 단계 더) → 메인 판정(남은 충돌을 파일로 직접 확인).
- **감사 중 프로젝트 파일 수정 0건.** 원 보고서 10개(r1-*·r2-*)는 세션 스크래치에 있고, 이 회의록이 정본이다.
- 기준: HEAD `db28a51` + 미커밋(v4.2). 계약서 **v4.2**. 글 1편(`posts/java/2026-09-21-note-125149.md`).

---

## 합의된 현황

### 이번 세션에 이행된 것 (회의와 병행, 미커밋)

| 작업 | 내용 | 검증 |
|---|---|---|
| v4.2 에디터 (#150-155) | 펜스 안 짝 넣기·건너뛰기·쌍 지우기 · Ctrl+/ 주석 토글 · Ctrl+D 줄 복제 · Alt+Shift+↑/↓ 줄 이동 · 선택 없는 Shift+Tab 결함 수정 · 코드 블록 언어 기억(`blogCodeLang`). 이전 세션이 코드를 먼저 썼고 이번에 계약 §6-2에 **사후 등재** | 헤드리스 합성 입력 32항목 통과. 실키보드·한글 IME 미확인 |
| #156 | `write.html` `#bodyHint` 문장을 v4.2 동작에 맞춤 | 계약 문장과 바이트 일치 |
| #157-159 푸터 둘째 줄 | 사용자 확정 문장 **`게시글은 모두 직접 작성하고 정리하였습니다`** · 세 페이지. §3·§10 등재, `components.css` 간격 두 값(문장 사이 8px · 연도 위 12px). 옛 미확정 줄("직접 손수")은 교체 | 세 페이지 360px에서 `.footer-note` 2개, 문장 일치, 틈 8px. `손수` grep 0건 |

### 결함이 아닌 것 (전원 확인)

- `innerHTML` 직접 대입 0건, CSS 리터럴 색 0건(절대 규칙 3·5 유지).
- CSRF: PUT·DELETE는 preflight 대상이고 서버에 CORS가 없어 막힌다(frontend-dev 2R). 단 앞으로 CORS를 "편의로" 열면 방어가 사라진다 → api.md에 금지 규칙으로 적는다.
- 글 id `note-125149`가 게시 시각과 다른 것은 결함이 아니다. `makeFallbackSlug`가 **write.html을 연 시각**을 한 번 고정한다(`editor.js:512-517`). 한글만 있는 제목은 이 때문에 사실상 충돌하지 않는다.
- 사용자 원문 *"괄호 **쓰면** … 닫히는거 까지 **같이** 보여줘"*의 해석은 **자동 닫기**로 확정한다(pm·dev-2·tutor 문법 판정 일치 — 조건절 "쓰면" + "같이"). 사용자에게 따로 묻지 않는다.

---

## 요구사항 대조표 (HANDOFF §1 기준 16개)

| # | 요구사항 | 상태 | 근거 · meeting-07 대비 |
|---|---|---|---|
| 1 | HTML·CSS·JS만 / Node 지양 | 충족 | `package.json`·`node_modules` 없음. 파이썬은 `server/`만 |
| 2 | 메모지 형태 | 기준선 폐기(사용자 v3.0 거부) | 문서마다 판정이 다른 것(PM-11)은 결정 기록처 부재 — 결함 D9 |
| 3 | 게시일 + 최종 수정일 | 부분 | 표시 로직 정상(`post.js:136-148`). ① index↔.md 시각 드리프트(D15) ② **같은 날 수정이면 숨기는 팀 규칙 때문에 공개 사이트에서 "수정일"이 한 번도 보인 적 없음**(N-4 → U8) ③ 덮어쓰기 시 새 글이 옛 글의 게시일을 물려받는 경로(D1) |
| 4 | 제목 작성 | 충족 | `write.html:145` |
| 5 | 표·그래프·텍스트·코드 | 부분 | 그래프=이미지인데 **이미지를 넣는 길이 없다**(업로드 B-4 증발, 상대 경로 함정 M-4) → U4 |
| 6 | 최신 반응형 | 미확인 | 헤드리스만. 실기기 5라운드째 → U7 |
| 7 | 애니메이션 | 충족 | 변화 없음 |
| 8 | 그때그때 직접 추가 | **부분 — 치명 결함** | 새 글 id가 겹치면 기존 글을 소리 없이 덮어씀(D1). 저장 경로 v1.1 실측 0회, 이 PC는 자동 커밋이 구조적으로 꺼짐(D2) |
| 9 | 가독성·구조·고퀄리티 | 부분 | 본문 목록 간격이 사용자 습관을 떨어뜨림(D16). 계약서 3,654줄·`editor.js` 3,116줄 |
| 10 | 흐르는 배경 + 애니메이션 | 미결 | 구현(메시)은 폐기됐지만 **요구사항 철회 여부는 답이 기록된 적 없음**(meeting-07 결정 4) → U9 |
| 11 | 외부 의존성 사전 승인 | 충족(코드) / 부분(스킬) | `slides`(Chart.js)·`ui-styling`(shadcn)이 규칙 1·2와 충돌하는 권고(PM-13) |
| 12 | Pages 공개 · 방문자 읽기 전용 | 충족 | 단 방문자에게 frontmatter 경고 토스트·오류 원문이 보임(D5·FD-7) |
| 13 | 개발→회의→개선→반복 | 부분 | 회의 사이·회의록→작업표 사이에 항목이 증발(D9) |
| 14 | 카테고리 = 폴더 | 충족 | |
| 15 | 폴더·파일 가독성 | 부분 | CLAUDE.md·HANDOFF·에이전트 정의·스킬이 서로 어긋남(D8·PM-8) |
| 16 | 푸터 문구 | 충족(작업 트리) | 첫 줄 원문 + 둘째 줄 사용자 확정. **커밋 전.** 둘째 줄 "모두 직접"이 에이전트 대필 경로와 충돌(D7) |

---

## 확정된 결함

표기: `묶은 1R/2R 항목` · 파일:줄 · 근본 원인 번호(아래 "근본 원인" 절).

### 치명

**D1. 새 글 id가 기존 글과 겹치면 서버가 기존 글을 소리 없이 덮어쓴다** — PM-1 · FD2-11ⓑ · N-1 · M-1 · R-A
- 경로(pm·dev·dev-2가 각각 추적, dev는 헤드리스로 slug 실측): `util.js:109-115 slugAscii`가 한글을 지운다 → `Java 정리`·`Java 복습`·`Java: 상속`이 같은 날 모두 `YYYY-MM-DD-java` → 에디터는 저장 전 존재 확인 없음(`editor.js:2109-2135`) → 서버는 upsert(`server/app.py:327`) → `write_atomic` 덮어씀(`:342`).
- PM이 1R에 적지 않았던 피해(dev·dev-2 2R):
  - 새 글이 **옛 글의 `created`를 물려받는다**(`app.py:331`).
  - 옛 글이 **다른 분류 폴더**에 있어도 지운다(`:344-348`).
  - 공유된 `post.html?id=`가 다른 글을 보여 준다.
  - 수정 중 id를 **기존 글 id로 바꾸면** A의 내용이 B의 게시일을 달고 저장되고 두 글이 다 망가진다 — **절대 규칙 4 직접 위반 경로**.
- 복구 수단 없음: 이 PC에는 `.env`도 저장소 로컬 `user.name`도 없어 자동 커밋이 매번 건너뛰어진다(D2). 토스트는 `저장했습니다` 한 단어 차이뿐(`editor.js:2240`), 응답의 `isNew:false`는 버려진다.
- 이 사용자의 제목 습관("Java …", 분류 `java`)이면 **다음 글에서 바로 날 수 있다**(tutor·dev-2).

### 중대

**D2. 유일한 저장 경로가 끝까지 실측된 적이 없고, 이 PC에서는 자동 커밋이 구조적으로 꺼져 있다** — PM-2 · FD-5 · N-1 · M-2 · R-B
- v1.0은 9/18 curl 실측 통과(`api.md:173`). **v1.1(자동 커밋·409 두 종류·Host 400) + 에디터 v3.9~v4.2 저장 흐름을 이은 실행은 0회.**
- 신원 없음 → 매 저장 `커밋 실패(커밋 신원이 없습니다 — .env에 …)` 약 100자가 상태줄에 붙는다(`app.py:176-177`). `CLAUDE.md:88` "자동으로 커밋한다"는 이 PC에서 거짓.
- `api.md:19` "골든 벡터가 store.js와 서버의 동일성을 고정한다"는 현재형 거짓 문장(벡터 없음). `api.md:197`이 잠금을 미룬 근거("자동 커밋으로 되돌린다")도 무너진다.

**D3. 에디터의 "코드 블록 안" 판정이 렌더러(marked 12.0.2)와 다르다** — FD-3 · FD2-4 · FD2-5 · FD2-13 · R-C · R-D
- dev가 입력 8개를 marked와 대조해 6개에서 갈림(`~~~`, 백틱 4개, 들여쓴 펜스, 목록 안 펜스, 줄 첫머리 인라인 ```).
- 결과: 코드 안 `//`+Enter가 펜스 3줄을 넣어 렌더를 깸(`editor.js:1182-1206`), 목록 이어쓰기가 코드 줄에 붙음, 툴바 코드 블록을 펜스 안에서 누르면 아래 문서 전체가 코드가 됨.
- v4.2 기능 다섯(짝·Enter·Ctrl+/·언어 기억·툴바)이 전부 이 판정 위에 서 있다. 계약 §6-2 "`~~~`는 세지 않는다(툴바가 만들지 않는 문법)"(contract:2261)가 생산자 기준 정의.

**D4. 한글 조합 직후 닫는 짝이 두 번 들어간다** — FD2-1
- 조합 가드(`editor.js:1657`)가 건너뛰기까지 꺼서 `print("안녕")")`. 로직 경로는 실측, 실제 IME 이벤트 순서는 미확인 → U7.
- 이 사용자의 코드 블록에는 한글 주석·문자열이 기본값(글 `:22,25,32,51,56`). 계약 contract:2121 "조합 중에는 어느 것도 발동하지 않는다"를 먼저 개정해야 한다.

**D5. 깨진 frontmatter가 방문자 화면에 그대로 나오고, 경고 토스트가 방문자에게도 뜬다** — FD-4 · N-5
- `---` 앞 빈 줄 하나로 메타 8줄이 본문·제목이 된다(`store.js:175-182`). 경고는 관리자 게이트 없음(`post.js:555-560`). 에디터 수정 모드로 열면 메타가 본문 칸에 들어가 저장 시 굳을 수 있음(dev-2 N-8, 미확인).
- **meeting-07:122 지시("frontmatter 경고 admin 게이트")가 계약 작업표 #124로 옮겨지며 빠졌다** — 이행자 누락이 아니라 번역 단계 누락(D9).

**D6. "계약 먼저" 게이트에 구멍이 셋이다** — PM-3 · WD-7 · FD2-10 · R-E
- ① 문구: 규칙 6·`/round-start`가 "클래스·구조"만 묻는다(round-start/SKILL.md:15) → 키 동작·사용자 문구가 빠진다. ② 위치: 메인 세션이 직접 투입하면 게이트를 우회 — v4.2도 푸터 옛 줄도 이 경로. ③ 도달: 금지 문장(contract:2111, 2208~)이 기본 읽기 2000줄 밖.
- 사후 등재가 비용 없이 통과해 IME·선택 보존·OS 키 충돌 설계 검토가 한 번도 없었다(dev-2 G3).

**D7. 에이전트가 글을 대신 써서 올리는 경로가 확정 푸터와 정면 충돌한다** — PM-5 · PM-7 · TU-14 · N-3 · M-3
- `new-post/SKILL.md:3` 발동 문구 "오늘 공부한 거 **정리해서 올려줘**", `edit-post`, 전역 `~/.claude/skills/learn/SKILL.md:44-50`(posts/·index.json 손편집) — 서버 단일 흐름·소유권·`created` 드리프트 원인(contract:2076)이기도 하다.
- 푸터 `게시글은 **모두** 직접 작성하고 정리하였습니다`가 공개되는 순간, 에이전트가 쓴 글 한 편이 사이트 문장을 거짓으로 만든다. tutor는 CLAUDE.md 4인 표·프로젝트 `.claude/agents/`에 없다(전역에만) → U2.

**D8. 에이전트 정의가 확정 결정과 반대로 지시한다** — PM-4 · FD2-17 · FD-16 · R-F
- `web-designer.md:28-29` "단색 배경 금지 — 그라디언트 메시·기울기" ↔ contract:280·1069(폐기). 이번 회의에 투입된 web-designer가 **그 정의로 떠서 계약서를 따라야 했다**(WD N9).
- `frontend-dev-2.md:45` `?admin=1`·localStorage 판정(폐기), `frontend-dev.md` 폐기된 `color` 필드·평면 `posts/`, "ES2020" ↔ 코드 전부 ES5. `config.js:42 storageKeys.admin`은 죽은 키.

**D9. 예약·지시·결정이 증발한다** — PM-6 · PM-11 · PM-12 · N-5 · dev-2 `storage` 이벤트 약속 · R-G
- 라운드 7 예약 4건(골든 벡터 M4-13 · 이미지 업로드 B-4 · editor.js 분리 M4-12 · index 재생성 B-3)이 취소 기록 없이 사라졌고 문서는 여전히 현재형(`api.md:19`, contract:243).
- `storage` 이벤트로 다른 탭 초안 감지(meeting-04:136, api.md:198 — dev-2 몫)는 0건 구현인데, 서버 잠금을 미룬 근거였다.
- 라운드 번호 체계 셋, 사용자 결정 기록처 없음.

**D10. 계약서가 단일 진실 공급원으로 기능하지 못한다** — PM-9 · WD-5 · WD-6 · WD-8 · N-6 · R-F
- 3,654줄, 이력 약 29%. 한 규칙을 여러 절이 각자 문장으로 반복(`.entry-cat` 숨김 조건 §4-4·§8·§11-1 셋 다 다름 등 낡은 교차 참조 11곳).
- 이번 개정에서도 기계적으로 결함이 생겼다: `### v4.1` 제목 중복(생겼다 해소), **머리말 contract:10 "CSS 변화 없음 · write.html은 `#bodyHint` 하나만" ↔ :14-20 푸터·CSS 추가**, contract:2198 "v4.1 (현재 write.html)"(메인 확인).

**D11. 에디터 작업 줄(상태줄)에 길이 예산이 없어 높이가 폭주한다** — WD-1 · N-7 · WD N11
- 저장 성공(경로 + 커밋 꼬리 + 링크 둘) · 보류 안내가 붙으면 784px에서 116/189, 1024 도킹에서 231/869px(1R 실측). D2 때문에 이 PC에서는 최악 조합이 **기본값**.
- 문장을 만드는 곳이 셋(계약 §6-1, `editor.js:2236`, `server/app.py:176-186` 최대 200자). §6-1 문구 목록에 4종 누락. 경로는 편집 배지와 중복.

**D12. 사이드바 고정 시 1024~1283px에서 `나란히` 두 창이 각 335px** — WD-2
- 계약이 스스로 "쓸 수 없다"고 한 폭. 고정 상태는 페이지를 넘어 이어져 에디터가 기본으로 도킹된 채 열린다(`theme-init.js:37-39`). §11-3은 목차엔 도킹 폭(1540−260)을 계산했지만 split에는 안 했다. 분기점이 CSS(`layout.css:829`)·JS(`editor.js:1768`, `ui.js:334`) 세 곳에 흩어져 있다.

**D13. v4.2 단축키를 눈으로 발견할 수 없고, 스크린리더 설명은 너무 길다** — WD-3 · PM-15 · FD2-9 · TU-12 · WD-4
- 보이는 통로 0개. `#bodyHint` 336자가 본문 칸 포커스마다 낭독. contract:2061 "마우스 사용자가 단축키를 발견하는 유일한 길이 툴팁"은 지금 거짓(v4.2 키 넷은 버튼·title 없음).
- tutor 보강: 이 사용자는 IntelliJ 키맵에 익숙하므로 안내의 핵심은 전체 표보다 **"IntelliJ와 다른 점"**(펜스 안에서만 짝·Ctrl+/, Esc→Tab 탈출, 줄 첫머리 `//`).

### 경미

| ID | 내용 | 근거 |
|---|---|---|
| D14 | 요약 카드가 실제 글에서 한 번도 안 뜨는데 M10을 ✔로 닫음. 글의 `핵심 정리:`는 h2가 아닌 **문단 라벨**(`.md:73`), `findRecapHeading`은 h2만(`post.js:235`). 계약 contract:1655는 콜론 처리(문단 습관)와 h2 요구를 한 행에 같이 적은 자기모순. 결함 본체는 완료 판정 방식(R-C) | FD-2 · PM-10 · TU-6 → **U3** |
| D15 | `index.json` created·updated(13:34:51) ↔ `.md`(13:34:03) 48초 드리프트. 화면 날짜는 같지만 관리자가 글을 열 때마다 경고 토스트 → 경고 무시 습관 | FD-1 · TU-15 → **U5(통보)** |
| D16 | **사용자의 주 작성 습관 "`라벨:` + 목록"을 본문 CSS가 떨어뜨린다.** `.prose p`와 `.prose ul`이 둘 다 `--sp-5`(20px, `prose.css:124, 215`)라 라벨과 목록 사이가 문단 사이와 같다. 느슨한 목록(항목 사이 빈 줄)은 `li > p` 규칙이 없어 항목 간격 20px, 같은 글의 촘촘한 목록은 8px(`:229`) | WD N1·N2(메인이 CSS로 확인, 픽셀 실측 전) |
| D17 | 문서 첫 글자가 `\n`이고 위치 0일 때 줄 시작 계산이 틀림 — 관용구 8곳 중 증상 5(Shift+Tab이 **다른 줄**을 고침, H2 버튼 IndexSizeError, 줄 이동 빈 줄 생성, 계기판 1행 0열), 잠복 1, 무해 2. **같은 식이 계약에 규칙으로 박혀 있다**(contract:2214) | FD2-2(2R에서 범위 정정) · N-2 |
| D18 | `//Java`·```` ```Java ```` 대문자를 소문자만 받는 정규식 셋이 무시(`editor.js:63,71,73`). 같은 파일의 주석 토글(`:1535`)·렌더러(`markdown.js:105`)는 대소문자 무시. 폴더 slug 규칙을 이유 없이 복사(contract:2190) | TU-11 |
| D19 | 선택 있는 Tab이 줄 전체 선택으로 끝남(계약 문장 contract:2128이 원인), 모든 편집 뒤 역방향 선택 방향 소실 | FD2-7 · FD2-6 |
| D20 | 주석 토글이 표에 없는 언어(powershell 등)에 `//`를 조용히 씀 | FD2-12 |
| D21 | Ctrl+S만 한글 키값 보정(`keyIs`) 밖(`editor.js:2916`) — 합성 이벤트만 재현 | FD2-3 |
| D22 | 두 탭이 초안 슬롯 `new`를 공유해 서로의 초안을 지움 | FD2-11ⓐⓒ |
| D23 | 렌더 JS 오류가 "불러오지 못했습니다"로 둔갑하고 오류 원문이 방문자에게 보임(`app.js:618-634`, `post.js`) | FD-7 |
| D24 | 본문 원시 HTML의 `id`·`class`·`data-*`가 살균 통과 — `class="modal is-open"`이 페이지를 덮고, 본문 id가 `aria-labelledby="postRelatedTitle"`을 가로채고, `data-admin-only`는 미리보기·로컬·공개 세 화면이 다름. 계약 contract:1549 "마크다운 결과라 클래스를 붙일 수 없다"는 거짓 전제 | FD-11 + WD 보강 |
| D25 | 서버 정적 서빙이 차단 목록이라 `.env`를 만드는 순간 `/.env`가 서빙됨(`app.py:34`), 대소문자 우회 미확인(`:430`). 자동 커밋이 `posts/` 전체를 쓸어 담고(`:178-186`), 분류 저장은 커밋 없음(`:378-394`) — 둘이 서로를 가림 | PM-14 |
| D26 | 제목·본문 칸 `spellcheck="false"`(`write.html:146, 285`, 계약 고정) — `settter` 같은 오타가 남은 원인 중 하나. 제목까지 끌 이유는 없음 | dev-2 N-3 |
| D27 | 이미지 경로 함정: `.md` 기준 상대 경로 `img/a.png`는 404, `/posts/…`는 Pages 프로젝트 사이트에서 404, 동작하는 표기는 `posts/java/img/a.png` 하나 | dev M-4 |
| D28 | 수정 모드에서 id를 바꾸면 옛 `post.html?id=` 링크가 끊김(칸이 열려 있음) | dev M-6 |
| — | 그 밖: 툴바 묶음 감김 360·1024(WD-9) · `--c-meta`≈`--c-warn`(WD-11) · 행간 리터럴 18곳·코드 칩 여백 변종(WD-12) · 계약 §0-2 "읽는 시간 폐기" ↔ 에디터 `읽는 데 약 N분`(WD-14) · 방문자 안내문이 내부 흐름 설명(WD-17, `write.html:116,120`) · index.json 구조 깨짐이 "글 없음"으로 보임(FD-8) · ISO 문자열 비교 정렬(FD-9) · 언어 없는 블록 라벨 `text`인데 자동 감지 색(FD-10) · 외부 링크 `//host` 누락(FD-13) · 문서 수치 불일치 24↔32 스킬, 13↔16 요구(PM-8) · 라운드 번호 셋(PM-12) · 규칙 1·2 충돌 스킬(PM-13) | |

### 글 내용 (사용자 데이터 — 에이전트는 고치지 않는다, U6)

- `posts/java/2026-09-21-note-125149.md:63-64` setter·getter 정의가 서로 뒤바뀜(같은 글 `:82-84`와 코드 주석은 맞음).
- `:43` 오타 `settter`, `:51` `읽거` — 목차와 제목 앵커 id(`h-…settter`)에 박힘. 지금 고치면 끊기는 링크 0개.
- `:38` "public: 외부에서 허용된 기능만" — public은 "어디서나 접근", 효과는 private 필드 + public 메서드 조합에서 나옴(tutor, 2R에서 경미로 낮춤).

---

## 근본 원인 (Why 연쇄 — 1R → 2R에서 한 단계씩 더)

| # | 근본 원인 | 묶인 결함 | Why 연쇄 |
|---|---|---|---|
| **R-A** | 쓰기 API가 의도(생성/수정)와 기준 버전 없이 덮어쓴다 | D1 · D22 | 왜 upsert? v3.9 서버 단일화 때 API를 다시 보지 않았다 → 왜 방치? 보호를 "자동 커밋으로 되돌린다"(api.md:197)에 미뤘다 → **그 복구 수단은 실측 0회이고 이 PC에서 성립하지도 않는다. 미검증 기능을 다른 결정의 안전 근거로 썼다.** 서버·에디터 두 층이 서로를 안전장치로 믿었다(dev-2 G2) |
| **R-B** | 저장 경로를 밟는 검증이 라운드 절차에 없다 | D2 · D11 | 에이전트 헤드리스는 start.ps1(API 없음)만 쓴다 → `/api-check`는 Docker 의존인데 Docker가 꺼져 있다 → Docker 없이 할 수 있는 클라이언트 절반(fetch 스텁 하네스)도 절차에 없다 |
| **R-C** | 완료 판정 = "코드가 계약 문장과 같은가". 실제 글·실제 입력으로 돌리지 않는다 | D3 · D14 · D17 · D18 · D19 | 계약서가 동작이 아니라 구현 공식(h2만, 백틱 줄 홀짝, `lastIndexOf` 식, "줄 전체 선택")을 적었다 → 왜? 계약서가 dev의 이행 판단을 글자 그대로 옮기는 기록부를 겸해 **코드 결함이 계약으로 역수입**됐다(contract:2214) → 왜 못 걸렀나? 픽스처를 **계약서를 읽고** 만들어 계약의 가정이 반드시 통과하는 순환(WD R3). 실제 글 사본을 검증 입력으로 삼으라는 규칙이 `/fixture`에 없다 |
| **R-D** | 한 규칙을 두 곳에서 따로 구현한다 | D3 · D18 · D25 · FD-5 | 판정을 내보내는 공용 함수도, 사본끼리 동일성을 재는 벡터도 없다 → "규칙의 집을 하나로"를 살균 경로에만 적용했다 |
| **R-E** | 계약 선행 게이트가 생김새에만, 라우팅 시점에만 걸린다 | D6 · D13 · D4 | 규칙 6 문구 → `/round-start` 라우팅 질문 → 메인 세션 우회 → 금지 문장이 2000줄 밖 → **동작 명세의 소유(web-designer)와 전문성(dev-2)이 어긋나** 순서를 지키면 느려지고, 느리면 순서가 무너진다(WD R2) |
| **R-F** | 문서가 현행과 이력을 가르지 않고, 같은 규칙을 사본으로 옮겨 적는다 | D8 · D10 · PM-8 | 에이전트 정의는 시스템 프롬프트라 계약서보다 먼저·강하게 읽힌다 → 틀린 정의가 가장 강한 문서가 된다 → 왜 사본을 grep하지 않나? **빌드 금지(절대 규칙 1)로 린터·CI가 하던 일관성 검사까지 함께 사라졌고, 대신할 절차를 넣지 않았다**(WD R1) |
| **R-G** | 회의 지시 → 계약 작업표 → 이행 확인 사이에 대조 단계가 없다 | D9 · D5 | 증발 지점 둘: ① 회의와 회의 사이(이월 절 없음) ② **회의록 지시가 작업표로 옮겨지는 순간**(meeting-07:122 → #124). 이행자는 표를 보고 ✔하므로 표에서 빠진 것은 "완료"와 구별되지 않는다 |
| **R-H** | 기능을 글쓴이의 실제 관습이 아니라 "글 0편" 시점 가정 위에 설계했다 | D14 · D16 · D18 · TU-7 | 요약 카드(h2), 템플릿(`config.js:88` — 실제 글 제목과 겹치는 것 0), 소문자 언어, 목록 간격 → 첫 글이 생긴 뒤 가정을 다시 대조하는 단계가 없었다 |

---

## 판정이 필요했던 충돌과 결론

| # | 충돌 | 결론 | 근거 |
|---|---|---|---|
| 1 | 펜스 판정 구현: FD **CommonMark 스캐너** vs FD2 **`marked.lexer` raw 누적** | **스캐너, `markdown.js`(frontend-dev 소유).** FD2는 2R에서 lexer안 철회. 계약은 방법이 아니라 **합격 기준**을 고정: FD-3 입력 8개 + 백틱 4개 중첩에서 `codeRanges` 결과 = marked 렌더의 `<pre><code>` 범위(헤드리스 대조) | lexer는 CDN 실패·오프라인에서 에디터 입력 규칙까지 죽이고, 목록 안 토큰의 원문 오프셋을 잃는다. 방법을 규약으로 삼으면 R-C를 반복한다(PM) |
| 2 | 409 `exists`를 어디에 띄우나: dev·dev-2 "자동 접미사 `-2`" vs designer "제목 칸 오류" | **자동 id(`idTouched` false) → `-2`…`-9` 접미사로 저장하고 결과 토스트에 한 줄로 알린다. 손으로 친 id → `#fIdError` 신설 + `.editor-more`를 열기만 하는 예외를 계약에 적는다.** 서버 409는 필수, 에디터 사전 확인은 보조이며 **분류 PUT보다 먼저** | 사용자가 짓지 않은 이름으로 일을 시키지 않는다(dev-2). 단 조용히 바꾸면 R4 "조용한 실패"(designer)라 알림은 남긴다. 분류 PUT 뒤 409면 빈 분류가 남는다(dev-2 보강 ⑤) |
| 3 | `#bodyHint` 길이: designer **두 문장 약 90자**(나머지는 단축키 표) vs dev-2 **"글자가 생기거나 사라지는 동작 + 탈출만" 약 200자** | **dev-2 기준 채택.** 명령(Ctrl+D·Alt+Shift·Ctrl+/·B/I)은 단축키 표로, 짝 자동 삽입·건너뛰기·`//` 변환·Enter 이어쓰기·Tab 탈출은 describedby에 남긴다 | 자동 삽입된 글자는 스크린리더 사용자에게 보이지 않는 변경이다. 누르지 않으면 안 일어나는 명령만 표로 보낸다 — 새 명령이 늘어도 설명이 길어지지 않는 상한 규칙이 된다 |
| 4 | WD-16 분류와 같은 태그 숨기기 | **철회(designer 본인)** | 태그 칩이 `?tags=` 교차 분류 필터의 유일한 입구(dev, `app.js:87-89`) |
| 5 | FD-12 `# 제목`이 있으면 전부 한 단 내리기 | **보류** | 목차(h2만 셈)와 요약 카드가 사라지는 퇴행(designer·tutor). 본문 h1 글 0편 |
| 6 | TU-9 검색이 본문을 안 봄 | **결함 아님 → 사용자 질문(U10)** | 요구사항 16개에 검색이 없고 "목록은 index.json만" 규칙과 충돌(dev). 진짜 원인은 요약 = 제목(글쓰기 안내 몫) |
| 7 | TU-11 대문자 심각도: tutor·dev-2 중대 vs PM 경미 | **경미(D18)**, 단 싸서 에디터 묶음에 넣는다 | 사용자는 `//Java`가 아니라 ```` ```Java ````를 손으로 친다(글 `:15,46`) — 관찰된 실패 경로가 아니다 |
| 8 | 우선순위: tutor "절차(TU-1·14) 먼저" vs PM "PM-1 먼저" | **D1 먼저** | 글 내용 수정은 사용자 손이라 병렬로 간다. 대필 경로는 U2가 선행 조건. 데이터 유실은 다음 글에서 바로 난다 |
| 9 | D25 `.env` 노출: PM "HIDDEN_FILES에 추가" vs dev "허용 목록" | **허용 목록**(첫 경로 조각 `index.html post.html write.html css js posts .nojekyll` + favicon, 대소문자 무시) | 차단 목록은 새 파일이 기본 노출. `.env`를 만드는 라운드(U1)와 같은 라운드에 |
| 10 | `expectedUpdated`(409 `stale`)를 이번에 넣나 | **넣는다(api.md v1.2의 선택 필드)** | 같은 PUT 변경이고, 잠금을 미룬 근거(api.md:197)가 무너졌다(dev) |
| 11 | 계약서 분리: designer "새 파일 없는 1단계 먼저" vs PM "`contract-history.md` 분리 승인" | **1단계 → 2단계 순서로 둘 다.** 조건: v4.2 커밋 뒤, 개정 인스턴스 하나일 때 | 이번 세션에 개정 인스턴스 둘이 겹쳐 머리말 모순이 생겼다 |
| 12 | created 드리프트: 사용자 승인 대기(3회째) vs 캐시 되돌림 | **통보 후 시행(U5)**, 반드시 **단독 커밋** | 정본은 `.md`(`store.js:306`, `app.py:247-262`), index는 파생 캐시(dev·PM 자기 정정). 커밋하지 않고 두면 다음 글 저장의 자동 커밋에 섞인다(D25) |
| 13 | WD-13 코드 블록 순백 배경을 사용자에게 묻나 | **묻지 않는다(designer 권한)** | 값이 `tokens.css` 안이고 §5-7-1에 판단 기록 있음 |
| 14 | FD-6 index 재생성기 심각도 | **경미, U4(예약 재개 여부)에 묶는다.** 재생성은 **메타 동기화 전용**(본문을 고친 글은 `updated`가 바뀌어야 한다 — tutor) | 손편집 뒤 재생성이 옛 수정일을 보존하면 그쪽이 거짓 수정일(PM 반박) |

**2R 자기 반박으로 철회·격하된 것**: TU-5·TU-8·TU-12 철회, TU-2 경미·TU-3 제안 · FD-2 경미·FD-14 철회·FD-12 보류·FD-11 수정(본문 id는 지우지 않고 `u-` 접두) · FD2-2 경미(범위 정정)·FD2-3 경미·FD2-8 Info·FD2-14 철회·FD2-18 닫음 · WD-4 최하·WD-7 저장 키 부분 철회·WD-10 대체·WD-13 일부 철회·WD-15 절반 철회·WD-16 철회 · PM-3 푸터 되돌리기 권고 철회·PM-11 "위임받은 결정"으로 정정.

---

## 다음 라운드 작업 지시 (라운드 9)

**순서**: ① web-designer의 v4.2 마무리 → **v4.2 확정 커밋**(사용자 승인) → ② pm(server·api.md)과 web-designer(계약) 병행 → ③ frontend-dev → ④ frontend-dev-2. 파일 소유 겹침 0.
이번 라운드 범위는 **P1 덮어쓰기 차단 · P2 v4.2 커밋 · P3 저장 경로 실측(U1 뒤) · P4 에디터 정확성 · P5 문서·절차 동기화 · meeting-07 누락분(D5) · D16**. 나머지는 아래 "이월 백로그"에 ID로 남긴다(R-G 재발 방지).

### web-designer (`docs/contract.md`, `css/*`)
1. **v4.2 마무리(커밋 전)**: §12-18 #158·#159 ✔ · 머리말 contract:10 "CSS 변화 없음 · `#bodyHint` 하나만" 문장 정정 · contract:2198 "v4.1 (현재 write.html)" 표 머리 정정.
2. **§6-0·§6-1 (D1)**: `#fIdError` 자리 + "접힌 `.editor-more` 안 칸의 오류는 펼친 뒤 포커스" 예외 · 409 `exists` 문구 · 자동 접미사 알림 문구 · 새 글 모드에서 `isNew:false` 경고 문구.
3. **§6-2 동작 정정 (D3·D4·D17·D18·D19·D20·D21)**: "펜스 안" = `Blog.markdown.codeRanges` 판정 + 합격 표(충돌 1) · `~~~` 문장 폐기 · 줄 시작을 동작 문장으로(contract:2214) · 언어 글자 대소문자 무시·**친 그대로 쓰기**·비교만 소문자 · 조합 확정 뒤 건너뛰기 보정(contract:2121) · Ctrl+S 글자 판정 = `keyIs` · 선택 있는 Tab은 원래 선택을 편집에 따라 옮김 · 주석 표에 powershell·bat·ini·lua 등 추가 + **모르는 언어는 무동작 + 토스트**.
4. **§5-6 (D5)**: "방문자는 메타 덩어리·파싱 경고를 보지 않는다. 경고는 관리자에게만" 화면 규칙 신설.
5. **§5-7 + `prose.css` (D16)**: `.prose p + ul, .prose p + ol { margin-block-start: var(--sp-2) }` · `.prose li > p { margin-block: 0 }` · `.prose li > p + p { margin-block-start: var(--sp-3) }` — 실제 글 사본으로 360/1024 실측 후 확정.
6. **§3-2**: `storageKeys.codeLang = 'blogCodeLang'` 등재(값 유지) · `storageKeys.admin` 삭제 등재.
7. 계약 §0에 **사용자 습관 목록**(```` ```Java ```` 대문자 · `라벨:` + 목록 · 느슨한 목록 · 코드 안 한글 · 사이드바 고정) 신설 — 모든 검증의 필수 입력(R-C).

### pm-integrator (`server/*`, `docs/api.md`, `CLAUDE.md`, `docs/HANDOFF.md`, `.claude/**`)
1. **`server/app.py` (D1·D25·D11)**: PUT 선택 필드 `ifNew: true` → 대상이 디스크(모든 후보 폴더)나 index에 있으면 `409 {code:"exists", path}` · `previousId`가 있고 새 id가 이미 있으면 **항상** 409 `exists` · `expectedUpdated` 불일치 → 409 `stale` · 정적 서빙 허용 목록(대소문자 무시) · 자동 커밋은 저장한 파일 + index/categories만 add, 분류 저장도 커밋 · `reason` 60자 상한, 긴 설명은 `detail`.
2. **`docs/api.md` v1.2**: 위 전부 + 오류 코드 표(`exists`·`stale`·`conflict` 구별) · `:19` 골든 벡터 "미구현" · "CORS를 열지 않는다" 규칙.
3. **`CLAUDE.md`**: 규칙 6 대상에 "사용자에게 보이는 문구·키 동작·저장 키" 추가 + "메인 세션도 같은 게이트를 따른다" · `posts/*` 소유를 "파생 파일 정합성(index·categories)만, 본문·frontmatter 값은 사용자"로 좁힘 · 스킬 수 정정 · `:88`에 커밋 신원 전제 · 행간이 규칙 5 "폰트" 범위인지 명시.
4. **`.claude/agents/*.md` (D8)**: 낡은 지시 교체, 디자인 원칙은 "계약 §1-2를 따른다" **참조**로, ES5 문체 + ES2015 내장 허용 명시.
5. **`.claude/skills` (D6·D7·D9·R-C)**: `round-start` 라우팅 질문을 "사용자에게 보이는 것, 또는 입력의 결과가 바뀌는가?"로 + 계약 끝까지 읽기(offset 분할) · `blog-meeting`에 "이월 백로그 · 결정 대기 · 회의 지시↔작업표 대조" 절 · `fixture`에 검증 입력 규칙(실제 글 사본 + 경계 문서 + 외부 마크다운) · `serve`에 첫 기동 전 신원 확인 · `new-post`·`edit-post`는 **U2 답에 따라** 폐기 또는 "write.html로 안내"로 · `contract-update`에 grep 일관성 검사 한 줄(폐기 이름 0건, `^### v` 중복, 머리말↔§13).
6. **U1 뒤 `/api-check` 전체 (P3)**: 한글 제목 새 글 · 같은 날 `Java 정리`→`Java 복습`(D1 재현 → 409) · 수정 모드 기존 id로 바꾸기(409) · created 불변 · 자동 커밋 파일 목록 · 손편집 파일이 커밋에 섞이지 않는지.
7. `docs/HANDOFF.md` 수치·흐름 정정(9/22 기준으로 낡음). 사용자 결정 기록 표를 HANDOFF에 신설(D9).

### frontend-dev (`index.html`, `post.html`, `js/{config,util,store,markdown,app,post,theme-init}.js`, `posts/*` 파생 파일)
1. **`Blog.markdown.codeRanges(text)` · `codeAt(text, pos)` 신설** — marked 없이 동작, 같은 text면 메모 1칸. 합격 = 충돌 1의 헤드리스 대조 벡터. `Blog.markdown.langLabel(lang)` export.
2. **D5**: `post.js:555-560` frontmatter 경고를 관리자 전용으로 + index에 없는 id일 때 문구 정정.
3. `config.js`: `storageKeys.codeLang = 'blogCodeLang'` 추가, `storageKeys.admin` 삭제(계약 6 뒤). `Blog.store.draft.key(id)` export.
4. **U5에 이의가 없으면** `posts/index.json:11-12`를 `.md:5-6` 값으로 — **단독 커밋, 서버 저장보다 먼저.**
5. 골든 벡터 `golden.json` 기대값 생성(헤드리스) → pm에게 전달(이월 M4-13 재개분 — U4가 취소면 생략).

### frontend-dev-2 (`write.html`, `js/{editor,ui,admin}.js`, `start.*`)
1. **먼저 기준선**: 스크래치에 `fetch` 스텁 저장 하네스(200 / 200+`isNew:false` / 409 `exists` / 500 / 네트워크 실패) — 지금 거동을 기록(R-B, Docker 없이 가능한 절반).
2. **D1 에디터 몫**: 분류 PUT **전에** `store.findMeta(id)` 사전 확인(충돌 2 규칙) · 새 글 PUT에 `ifNew`, 수정 PUT에 `expectedUpdated` · `apiErrorMessage`를 `error.code`로 분기(`conflict`에만 "손으로 정리") · `isNew:false` 경고. (api.md v1.2 · 계약 2 뒤)
3. **D17**: `lineStartAt(value, p)` 하나로 8곳 교체(계약 3 뒤).
4. **D3**: `insideFence`·`codeLines`·`fenceLangAbove`·`learnFenceLang`·`insertCodeBlock` 가드를 `codeAt`으로 교체, 판정 사본을 에디터에 남기지 않는다(frontend-dev 1 뒤).
5. **D18·D19·D20·D21·D4**: 계약 3대로. D4(IME 보정)는 **U7 실기 확인 뒤**, 조합 가드가 돌려보낸 경우에만 플래그로 다음 `input` 한 번 보정.
6. `write.html:116, 224` 낡은 주석 정리 · `blogCodeLang` → `CFG.storageKeys.codeLang`(폴백 유지).

### 이월 백로그 (다음 회의가 반드시 받는다 — ID로 추적)

| ID | 내용 | 담당 | 선행 |
|---|---|---|---|
| B8-1 | D13 단축키 표(`#btnKeys` + `<template>` + 기존 모달) + `#bodyHint` 축소(충돌 3 기준) + contract:2061 정정 · WD-9 툴바 묶음 재편과 함께 | designer → dev-2 | — |
| B8-2 | D11 §6-1 상태줄 닫힌 문구 표(문장·최대 길이·자리) · 행동 슬롯 · 결정 보류는 고정 길이 `임시저장본` 버튼 | designer → dev-2 | P1 서버 reason 상한 |
| B8-3 | D12 §11-3 "도킹 × split"(도킹 시 1284) + split 판정은 CSS 결과를 JS가 읽음(`ResizeObserver`) | designer + dev-2 같은 라운드 | — |
| B8-4 | D24 살균 훅: 본문 `class`·`data-*` 제거(`language-*` 예외), 본문 `id`에 `u-` 접두 · contract:1549 정정 · D23 렌더 오류 분리 · FD-10 plaintext · FD-13 프로토콜 판정 | designer → dev | — |
| B8-5 | D22 초안: `버리기` = 보류로 옮기기, 최근 `savedAt` 경고, `storage` 이벤트(meeting-04:136 약속) | dev-2 | `store.draft.key` |
| B8-6 | D10 계약 슬림화 1단계(머리말 → §13, §12 ✔ 표 삭제, 취소선 정리) → 2단계 `contract-history.md` | designer · pm | v4.2 커밋, 개정 인스턴스 하나 |
| B8-7 | D26 제목 칸 spellcheck 기본값(계약 개정) · 본문은 U11 | designer → dev-2 | — |
| B8-8 | D27 `renderInto(…, {assetBase})` 상대 이미지 경로 | designer → dev · dev-2 | U4 |
| B8-9 | D28 수정 모드 id 칸 읽기 전용 + "id 바꾸기(링크가 끊깁니다)" 확인 | designer → dev-2 | — |
| B8-10 | D14 요약 카드 문단 라벨형(`.prose` 직계 p `핵심 정리`만 + 바로 뒤 ul/ol, p를 h2로 바꾸지 않음) | designer → dev | **U3 = (a)일 때만** |
| B8-11 | 라운드 7 예약 4건(골든 벡터 · 이미지 업로드 · editor.js 분리 · index 재생성) | 전원 | **U4** |
| B8-12 | FD-4 ① frontmatter 관용 파싱(여는 `---` + META_KEYS 줄만) · FD-9 `Date.parse` 정렬 | dev + pm(posts.py 동시) | 골든 벡터 |
| B8-13 | 경미 잔여: WD-11 · WD-12(`--code-chip-pad`·`--lh-ui`) · WD-14 · WD-17 · FD-8 · FD2-6 · PM-8 · PM-12 · PM-13 | 각 소유자 | — |

---

## 사용자 결정 필요 사항

| # | 종류 | 질문 | 기본값(답이 없을 때) |
|---|---|---|---|
| **U1** | 조치 | Docker Desktop을 켜고 커밋 신원을 한 번 설정해 주세요 — 프로젝트 폴더에서 `git config user.name`·`user.email`, 또는 `.env`에 `BLOG_GIT_NAME`·`BLOG_GIT_EMAIL`. 없으면 저장은 되지만 커밋이 매번 건너뛰어져 덮어쓴 글을 되돌릴 이력이 없습니다 | P3 실측 불가로 남김 |
| **U2** | 결정 | 푸터 "게시글은 **모두** 직접 작성하고 정리하였습니다"와 에이전트 대필 경로(`/new-post` "정리해서 올려줘", 전역 `/learn` 저장, tutor)가 충돌합니다. (A) 대필 경로 폐기 — tutor는 설명·오류 지적만, 글은 직접 write.html에서 (B) 유지하고 푸터 문장을 다시 정함 | **(A)** 권고. 전역 `~/.claude/` 파일은 프로젝트 밖이라 직접 고치시거나 허락이 필요 |
| **U3** | 결정 | 글 끝의 `핵심 정리:`를 (a) 글 맨 위 요약 카드로 옮겨 보여 줄까요(본문 끝에서는 사라짐) (b) 요약 카드 기능을 없앨까요 (c) 지금처럼 템플릿의 `## 다시 볼 때 이것만`일 때만 둘까요? | (c) |
| **U4** | 결정 | 라운드 7에서 사라진 예약 4건(골든 벡터 · 이미지 업로드 · editor.js 분리 · index 재생성)을 재개할까요, 취소를 기록할까요? 이미지 업로드를 취소하면 그래프는 "`posts/<분류>/img/`에 직접 넣고 `posts/java/img/a.png`처럼 쓰기"로 확정됩니다 | 이미지는 "규칙만", 나머지 재개 |
| **U5** | 통보 | `index.json`의 게시·수정 시각을 원본 `.md` 값(13:34:03)으로 맞춥니다. 화면 날짜(2026.09.21)는 그대로입니다. **이의가 있을 때만** 답해 주세요 | 시행(단독 커밋) |
| **U6** | 조치 | 공개 글 `:63-64` setter·getter 정의가 뒤바뀌어 있고 `:43` `settter` 오타가 있습니다. 푸터 문장 때문에 에이전트는 고치지 않습니다. 에디터로 고치면 수정일이 오늘로 바뀝니다 | — |
| **U7** | 조치(10분) | 실기 확인: ① 코드 블록 안에서 `("안녕")` 입력 시 닫는 따옴표·괄호가 두 번 생기는지 ② Alt+Shift+↑가 입력 언어를 바꾸는지 ③ 한글 모드 Ctrl+S가 브라우저 저장 창을 여는지 ④ 폰으로 목록·글 한 번 | 미확인 유지 |
| **U8** | 결정(경미) | 같은 날 수정한 글의 "수정일"을 (A) 지금처럼 숨길까요 (B) 요구사항 #3 문자 그대로 항상 표시할까요? | (A) |
| **U9** | 결정 | 요구사항 #10 "배경이 자연스럽게 흐르는 느낌"을 철회하시나요? 현재는 종이색 단색이고 기록에 답이 없습니다 | "현행 유지, #10 미결"로 기록 |
| U10 | 선택 | 목록 검색이 글 본문 속 낱말(예: protected)까지 찾아야 하나요? 태그에는 분야(Java)와 개념(캡슐화) 중 무엇을 적으실 건가요? | 현행 |
| U11 | 선택 | 본문 칸 맞춤법 검사를 켤까요? 켜면 코드 식별자에도 밑줄이 쳐집니다(제목 칸은 켜는 것으로 진행) | 본문 꺼짐 유지 |

사용자에게 묻지 않기로 한 것: 괄호 원문 해석(자동 닫기로 확정) · 코드 블록 배경색(디자이너 권한) · 태그 중복 숨김(철회).
